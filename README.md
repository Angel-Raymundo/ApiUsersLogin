To run the project, you must create a database called usuariosdb.

Make sure that the username and password match the ones you configured in MySQL.
You can do this in src/main/resources/application.properties.
